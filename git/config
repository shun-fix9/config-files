[color]
  ui = auto
[merge]
  tool = vimdiff
[alias]
  a = add -A
  ci = commit -v
  co = checkout
  m = checkout master
  br = branch
  tmp = stash save -u

  up = "!git remote update --prune && git fetch --tags --prune && git m && git pull"
  status-verbose = "!git branch && git status && git stash list"

  user-fix9 = "!git config --local user.name shun && git config --local user.email shun.fix9@gmail.com"
  reg-pub-fix9 = reg-pub github:shun-fix9 bitbucket:shun-fix9 gitlab:shun-fix9-groups

  setup = "!git user-fix9 && git reg-pub-fix9"
[push]
  default = current
[user]
  useConfigOnly = true
[credential]
  helper = store --file=/home/shun/.git-post/credentials
